export default function Home() {
  // The Worker serves public/index.html at / so the established static site
  // remains the single content source. This component satisfies the App Router
  // build contract without changing any visitor-facing content.
  return null;
}
